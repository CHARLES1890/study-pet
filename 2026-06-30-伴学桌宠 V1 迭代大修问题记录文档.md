# 伴学桌宠 V1 迭代大修问题记录文档
## 文档说明

本文档记录当前版本 5 项核心缺陷：现象、底层错因、架构反思、修复方案，用于统一迭代重构，规避同类耦合问题再次出现。

## 一、问题 1：数据统计导出功能易用性差，用户无法确认导出结果、找不到文件

### 1.1 问题现象

1. 点击导出无成功 / 失败弹窗提示，无法判断操作是否执行完成；
2. 未展示导出文件本地完整路径，普通用户找不到导出文件；
3. 无快捷打开导出目录的操作入口。

### 1.2 底层错因

1. 文件 IO 逻辑全部耦合在前端 Vue 组件内，未下沉至 Rust 统一封装；
2. 导出流程缺少完整反馈链路：执行中 loading、成功提示、失败捕获、目录跳转；
3. 导出目录未做统一固定规划，路径分散无记忆规则。

### 1.3 反思

Demo 阶段仅关注 “能导出文件”，忽略普通用户无开发环境、不懂文件目录逻辑；

未遵循分层规范：系统文件操作属于底层 IO 能力，不应交给短命 WebView 页面单独处理。

### 1.4 修复方案

1. 将导出文件读写、目录创建逻辑迁移至 Rust 底层接口；
2. 导出流程补充三层交互反馈：按钮加载态、成功弹窗（展示完整路径）、异常报错弹窗；
3. 弹窗增加「打开导出文件夹」按钮，调用 Tauri 系统 API 直接唤起资源管理器；
4. 统一固定导出目录：`app_data_dir/export/`，自动创建目录。

## 二、问题 2：专注计时功能，窗口隐藏 / 切换页面后计时直接重置清零

### 2.1 问题现象

1. 切换软件内其他功能 Tab，计时从头开始；
2. 将桌宠窗口隐藏、最小化后，计时丢失，重新显示清零。

### 2.2 底层错因

1. 定时器`setInterval`、计时时长变量全部写在 Vue 页面组件（WebView 前端）；
2. WebView 遵循浏览器沙箱生命周期：路由切换 / 窗口隐藏时 JS 执行环境被销毁，定时器、内存变量全部释放；
3. 长驻后台任务与界面视图强耦合，未解耦下沉至 Rust 主进程。

### 2.3 反思

前期未区分「界面层」与「底层常驻服务」边界，把需要长期稳定运行的长任务放在生命周期极不稳定的前端；

对 Tauri 两层进程生命周期认知不足，误以为页面代码可以长期后台执行。

### 2.4 修复方案

1. 完整重构计时逻辑：将计时状态、异步定时器迁移至 Rust 主进程 Tokio 后台线程；
2. 前端仅做视图展示：定时轮询从 Rust 拉取最新计时数值，不维护任何计时状态；
3. 程序启动自动读取本地缓存计时记录，实现断点续计；
4. 拦截窗口关闭事件，改为仅隐藏窗口、托盘常驻，避免 WebView 整体销毁。

## 三、问题 3：功能界面拖动交互逻辑反直觉，用户难以理解操作方式

### 3.1 问题现象

窗口拖动仅支持点击顶部中间白色窄条区域，其余顶部区域无法拖动，普通用户无法识别可操作区域。

### 3.2 底层错因

1. 拖拽区域 DOM 范围设计过小，无视觉交互提示；
2. 未区分功能按钮区与拖拽区域，拆分不合理，操作门槛高。

### 3.3 反思

Demo 开发仅满足自身操作，未站在普通用户视角做可用性设计；缺少基础 UI 交互提示与大面积可操作区域设计思维。

### 3.4 修复方案

1. 将整个顶部标题栏全部设为拖拽响应区域，扩大可点击范围；
2. 鼠标悬浮顶部时切换`move`移动光标，提供视觉引导；
3. 顶部功能按钮独立隔离，不与拖拽区域冲突，避免误触。

## 四、问题 4：页面上下滚动条过细，鼠标难以点击滑动

### 4.1 问题现象

列表滚动条宽度极小，鼠标很难精准捕获滑块，滑动操作体验极差。

### 4.2 底层错因

直接使用浏览器原生默认滚动条，未自定义滚动条 CSS 样式，适配桌面端鼠标操作。

### 4.3 反思

照搬网页端默认样式，未考虑桌面软件鼠标操作场景的交互适配；只完成功能可用，忽略桌面端易用性细节。

### 4.4 修复方案

1. 全局自定义滚动条样式，加宽轨道与滑块尺寸；
2. 增加 hover 高亮效果，提升辨识度与点击容错面积；
3. 统一全页面滚动条风格，保持视觉一致。

## 五、问题 5：无桌宠自定义缩放功能，无法调整显示大小

### 5.1 问题现象

当前仅固定尺寸渲染桌宠形象，不支持放大、缩小自定义调节，无配置面板。

### 5.2 底层错因

1. 未设计全局尺寸配置项，无本地持久化存储缩放数值；
2. 未预留 CSS 缩放布局逻辑，缺少设置面板交互控件。

### 5.3 反思

前期需求仅完成基础展示，未考虑用户个性化自定义需求；全局配置体系缺失，未统一规划用户设置持久化方案。

### 5.4 修复方案

1. 设置面板新增缩放滑块，调节区间 0.5 ~ 2.0 倍；
2. 缩放数值统一存入本地配置 JSON 文件，Rust 读写持久化，重启自动加载；
3. 使用`transform: scale()`实现整体缩放，同步修正拖拽坐标偏移问题；
4. 增加边界限制，防止尺寸超出屏幕可视范围。

# 整体大修核心架构反思

## 1. 核心错误根源

1. 开发模式为 Vibe Coding 快速堆 Demo，未提前制定分层规范，**所有 IO、长任务、全局状态全部耦合在 WebView 前端**；
2. 对 Tauri 双进程生命周期认知缺失：WebView 前端依附页面、生命周期脆弱，Rust 主进程常驻后台，本应承担底层能力；
3. 只以 “开发者本地能跑通” 为标准，未站在普通用户、多场景、长期迭代的工程视角设计。

## 2. 分层架构整改硬性规范（后续开发强制遵守）

1. **视图层（Vue 前端）**：仅负责渲染页面、接收用户点击、调用底层接口；禁止写定时器、文件读写、全局持久状态；
2. **通信层**：统一封装`invoke`调用工具，隔离前端与 Rust 底层；
3. **底层服务层（Rust）**：承载三类逻辑：
    
    - 长驻任务：计时、后台循环统计；
    - 底层 IO：配置读写、文件导出、本地 JSON 持久化；
    - 全局状态：缩放尺寸、自定义鼓励语、计时缓存。
    

## 3. 后续新增功能前置校验规则

新增任何功能前先判断：是否包含定时任务 / 文件读写 / 全局持久配置，满足任意一条，逻辑必须下沉 Rust，禁止写在前端组件内。

## 4. 长期工程感悟

技术选型与前期分层架构规划优先级远高于业务代码编写；前期缺少规范会产生不可逆的代码耦合债务，后期修复成本指数级上涨；Demo 可用 ≠ 工程可落地、可维护。

- 动手前先写一页简单架构分层文档，规定什么逻辑放 Rust、什么只能放前端；
- 底层公共工具优先写完（文件读写、计时服务），再写页面；
- 每写完一个功能，主动测边界场景：切页面、隐藏窗口、重启软件；
- 每次更新前收集所有问题统一记录，集中迭代更新，零散修复容易遗漏。